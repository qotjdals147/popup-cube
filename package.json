{
  "name": "popup-cube",
  "version": "0.2.0",
  "private": true,
  "description": "POP-UP CUBE — 2D pixel metaverse pop-up commerce platform (Turborepo monorepo)",
  "workspaces": [
    "apps/*",
    "packages/*",
    "server"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@11.16.0",
  "overrides": {
    "metro": "0.81.5",
    "metro-config": "0.81.5",
    "metro-resolver": "0.81.5"
  }
}
