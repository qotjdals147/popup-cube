import { CartWebViewScreen } from '../../src/components/CartWebViewScreen';

/** §60 — 장바구니 탭 (WebView `/app/cart` · 매장과 동일 localStorage) */
export default function CartScreen() {
  return <CartWebViewScreen />;
}
